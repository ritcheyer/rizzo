.wv--nav--inline.nav__item--user-menu.js-user-signed-out
  - user_navigation_items[:signed_out_links].each do |item|
    = render "components/navigation/nav_item", item: item, active: false, primary: true

.wv--nav--inline.nav__item--user-menu--wv.js-user-signed-out
  - user_navigation_items[:signed_out_links].reverse.each do |item|
    = render "components/navigation/nav_item", item: item, active: false, primary: true
