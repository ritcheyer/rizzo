= render 'styleguide/partials/shared'

.card.styleguide-intro.styleguide-block.copy--body.context--content
  %h1.copy--h1 Usabilla

  %p
    This components drops in a floating widget for users to leave feedback on the site.
  %p


.styleguide-block

.styleguide-block__description.card.copy--body

  %h2.copy--h2.styleguide-block__heading Setup

  %p
    The widget is really easy to set up. Just require the library and that's it.

  %pre.styleguide.copy--body.language-javascript
    %code
      = preserve do
        :plain
          require([ "lib/components/usabilla" ], function(Usabilla) {
            new Usabilla();
          });
