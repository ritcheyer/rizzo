- if properties[:tags] && properties[:tags][:top_choice?]
  %span.badge.badge--outline.badge--small.badge--blue
    Top Choice

- elsif properties[:tags] && properties[:tags][:lp_reviewed?]
  %span.badge.badge--outline.badge--small.badge--green
    LP Reviewed
