.widget__promo.context--content

  %h2.copy--h1{ class: properties[:title_class] || false }
    = properties[:title]
    %span.copy--feature
      = properties[:description]

  .nav--inline
    - 3.times do
      .nav__item
        = ui_component("ads/mpu")
